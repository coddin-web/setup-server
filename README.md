# Set up a server with some basic tooling
A simple batch script to setup a server

The `install.sh` will install a few tools we always use on servers:
- zsh
- ohmyzsh
- zsh-autosuggestions
- zsh-syntax-highlighting
- ruby
- rmate
